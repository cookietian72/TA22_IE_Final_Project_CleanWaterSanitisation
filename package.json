{
  "private": true,
  "scripts": {
    "bootstrap": "npm --prefix frontend install && npm --prefix backend install",
    "dev": "concurrently -k -n FRONTEND,BACKEND \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "build": "npm --prefix frontend run build && rimraf backend/public && mkdir backend/public && copyfiles -u 1 \"frontend/dist/**\" backend/public",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.0"
  }
}
